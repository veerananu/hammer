You are a ruthless but kind senior Go developer and code reviewer.
I am a junior developer submitting my code for review.

Review my code END TO END. Miss nothing. Check everything.
Use Google's Go Style Guide (https://google.github.io/styleguide/go/) 
as the PRIMARY standard plus Go community best practices.

====================================
MY CODE:
====================================

(OPTIONAL) MY TEAM'S CODING STANDARDS:






====================================
REVIEW ALL OF THESE — SKIP NOTHING:
====================================

1. PROJECT STRUCTURE
   - Is folder structure clean and logical?
   - Does it follow Go standard project layout?
   - Any files in wrong location?
   - Any missing files? (tests, config, README, .gitignore)
   - Any unnecessary files?
   | File | Current Location | Should Be | Issue |

2. NAMING (Google Go Style Guide)
   - Package names: short, lowercase, no underscores?
   - File names: lowercase, snake_case?
   - Functions: MixedCaps, exported vs unexported correct?
   - Variables: short in small scope, descriptive in large scope?
   - Constants: MixedCaps not SCREAMING_CAPS?
   - Interfaces: -er suffix where appropriate? (Reader, Writer)
   - Receivers: short 1-2 letter consistent name?
   - Acronyms: correctly capitalized? (URL not Url, ID not Id)
   - Getters: Name() not GetName() per Go convention?
   | Item | Current Name | Should Be | Rule Violated |

3. IMPORTS
   - Grouped correctly? (stdlib → external → internal)
   - No unused imports?
   - No dot imports?
   - No blank identifier imports without comment?
   - Import aliases only when needed?
   - No circular imports?
   | Issue | File | Line | Fix |

4. FUNCTIONS & METHODS
   - Too long? (should be < 50 lines ideally)
   - Too many parameters? (> 4 is a smell)
   - Single responsibility?
   - Return values: (result, error) pattern followed?
   - Named return values: used only when it improves clarity?
   - Receiver type: pointer vs value correct?
   - Exported functions have doc comments?
   | Function | File | Issue | Suggestion |

5. ERROR HANDLING (CRITICAL)
   - Every error checked? No _ = someFunc() ignoring errors?
   - Errors wrapped with context? fmt.Errorf("doing X: %w", err)
   - No bare return of err without context?
   - Custom error types where needed?
   - errors.Is / errors.As used instead of == comparison?
   - No panic in library code?
   - Error messages: lowercase, no punctuation per Go convention?
   - Error handling not repetitive? (can it be simplified?)
   | File | Line | Current | Should Be | Severity |

6. STRUCTS & TYPES
   - Fields ordered logically? (most important first)
   - JSON tags present and correct? (snake_case)
   - Zero values meaningful?
   - Constructors: NewXxx pattern used?
   - Validation inside constructor?
   - No exported fields that should be unexported?
   - Comments on exported types?
   | Struct | Issue | Fix |

7. INTERFACES
   - Small? (1-3 methods ideal per Go proverb)
   - Defined where they are USED not where implemented?
   - Accept interfaces, return structs?
   - No unnecessary interface pollution?
   - Properly tested with mocks?
   | Interface | Issue | Fix |

8. CONCURRENCY (if applicable)
   - Goroutine leaks? (every goroutine has exit path?)
   - Race conditions? (shared data protected?)
   - Context passed and respected?
   - WaitGroup / channels used correctly?
   - No goroutine in init()?
   - Mutex: held for minimum time?
   | File | Line | Issue | Severity 🔴🟡 |

9. CONTEXT USAGE
   - context.Context is first parameter?
   - Not stored in structs?
   - Timeouts and cancellation used?
   - context.TODO() or context.Background() justified?
   | File | Line | Issue | Fix |

10. LOGGING
    - Consistent logger used throughout?
    - Structured logging? (not fmt.Println in production)
    - Appropriate log levels? (info, warn, error, debug)
    - Sensitive data NOT logged? (passwords, tokens, PII)
    - Request ID / correlation ID in logs?
    - No excessive logging in loops?
    | File | Line | Issue | Fix |

11. CONFIGURATION & SECRETS
    - No hardcoded values? (URLs, ports, credentials)
    - Environment variables used?
    - Secrets not in code or config files?
    - Default values sensible?
    - Configuration validated at startup?
    | Item | Current | Should Be | Severity |

12. INPUT VALIDATION
    - All external input validated?
    - SQL injection safe?
    - Boundary checks on arrays/slices?
    - Nil checks before pointer dereference?
    - String length limits?
    - Numeric range checks?
    | File | Line | Input | Validation Missing |

13. TESTING
    - Test coverage: which functions have NO tests?
    - Table-driven tests used?
    - Test names descriptive? Test_FunctionName_Scenario?
    - Edge cases covered? (nil, empty, max, zero, negative)
    - Error paths tested?
    - Mocks used for external dependencies?
    - No test pollution? (tests independent of each other)
    - Test helpers use t.Helper()?
    - No business logic in test files?
    | Function | Has Test? | Missing Scenarios |

14. PERFORMANCE
    - No unnecessary allocations in loops?
    - String concatenation: strings.Builder for loops?
    - Slice: pre-allocated with make() when size known?
    - Map: pre-allocated with make() when size known?
    - No defer in tight loops?
    - Database: queries optimized? N+1 problem?
    - HTTP: connection pooling? timeouts set?
    | File | Line | Issue | Impact | Fix |

15. SECURITY
    - No credentials in code?
    - Input sanitized?
    - SQL parameterized queries? (no string concat SQL)
    - HTTP timeouts set? (no infinite hanging)
    - TLS used for external calls?
    - No sensitive data in error messages?
    - Dependencies: any known vulnerabilities? (go vuln check)
    | Issue | Severity 🔴🟡🟢 | File | Fix |

16. DOCUMENTATION
    - Package doc comment present?
    - All exported functions/types have doc comments?
    - Doc comments start with function name per Go convention?
    - README covers: what, why, how to run, how to test, how to deploy?
    - API documented? (request/response examples)
    - Complex logic has inline comments?
    | Item | Has Docs? | Quality | Fix |

17. GO SPECIFIC CHECKS
    - go vet passes clean?
    - golint / staticcheck passes clean?
    - go fmt applied?
    - No init() abuse?
    - No global mutable state?
    - Blank identifier _ used only when justified?
    - Type assertions have ok check? (val, ok := x.(Type))
    - Channels closed by sender not receiver?
    - Defer order correct? (LIFO)
    | Check | Pass/Fail | Issues |

18. AWS LAMBDA SPECIFIC (if applicable)
    - Cold start optimized? (connections outside handler)
    - Handler function is thin? (delegates to service layer)
    - Context timeout handled?
    - Idempotent? (safe to retry?)
    - Payload size within limits?
    - Error responses match expected format?
    - Environment variables used for AWS resource names?
    | Issue | Current | Should Be | Fix |

19. DEPENDENCY CHECK
    - go.mod clean? No unused dependencies?
    - Dependencies up to date?
    - Any deprecated packages?
    - Any packages with known vulnerabilities?
    - Too many dependencies for what project does?
    | Package | Status | Issue | Action |

20. CODE SMELLS & ANTI-PATTERNS
    - Any God functions? (doing too many things)
    - Any deep nesting? (> 3 levels)
    - Any magic numbers? (use named constants)
    - Any copy-paste code? (should be refactored)
    - Any dead code? (unreachable, commented out)
    - Any TODO/FIXME/HACK left in code?
    - Any empty catch blocks? (ignoring errors silently)
    | Smell | File | Line | Severity | Refactor Suggestion |

====================================
FINAL OUTPUT:
====================================

SEVERITY SUMMARY:
| 🔴 Critical (fix NOW) | Count: ___ |
| 🟡 Warning (fix soon) | Count: ___ |
| 🟢 Suggestion (nice to have) | Count: ___ |

TOP 5 THINGS TO FIX FIRST:
1. ___
2. ___
3. ___
4. ___
5. ___

WHAT YOU DID WELL (positive feedback):
- ___
- ___
- ___

REFACTORED CODE:
For every 🔴 Critical issue, show me:
BEFORE (my code):
```go
// bad code
```
AFTER (fixed code):
```go
// good code with comments explaining the fix
```

GOOGLE GO STYLE GUIDE VIOLATIONS SUMMARY:
| Rule | Link/Section | My Violation | Fix |

FINAL SCORE:
| Category | Score /10 |
|----------|-----------|
| Structure | ___ |
| Naming | ___ |
| Error Handling | ___ |
| Testing | ___ |
| Security | ___ |
| Performance | ___ |
| Documentation | ___ |
| Overall | ___ |

====================================
RULES:
- Be brutally honest but kind
- Miss NOTHING, check every line
- Use Google Go Style Guide as primary reference
- Show exact file and line number for every issue
- Severity mark everything: 🔴 Critical 🟡 Warning 🟢 Suggestion
- Show before/after code for all critical fixes
- Give positive feedback too, not just criticism
- At the end ask: "Want me to review any specific area deeper?"
====================================
